[tox]
envlist = py27, py35

[testenv]
passenv=SWAK_HOME
deps=
    -r{toxinidir}/requirements.txt
    -r{toxinidir}/requirements-dev.txt
commands=
    coverage run --source swak -m pytest tests
    coverage report
